#include <stdio.h>
/**
 * main - Entry point
 * @argc: size  of argv
 * @argv: array of size argc
 * Return: Always 0 Success
 */
int main(int argc, char* argv[])
{
	printf("%d\n", (argc-1));

	return(0);
}
